
export enum UserRole { USER = 'user', PROMOTER = 'promoter', ADMIN = 'admin' }

export enum ActivityType { ROAD_TRIP = 'road-trip', EVENT = 'event' }

export enum RequestStatus { PENDING = 'pending', REJECTED = 'rejected', ACCEPTED = 'accepted' }

export enum ConversationType { GROUP = 'group', USER2USER = 'user2user' }

export enum PlaceType {
  RESTAURANT = 'restaurant',
  MONUMENT = 'monument',
  HOTEL = 'hotel',
  MUSEUM = 'museum',
  PARK = 'park',    
}

export enum NotificationPriority { LOW = 'low', NORMAL = 'normal', HIGH = 'high' }
